# 交接：支付收款方案（2026-06-25 晚）

## 一句话现状
支付宝「当面付」走不通——**根因是账号没资质，不是代码/密钥问题**。已改用「个人收款码 + 后台人工开通会员」过渡方案，**代码全部写好并已提交本仓库**，但**线上部署被还原了**（疑似多 AI 工具互相覆盖），还需重新部署一次并做外网验证。

---

## 一、核心结论：为什么之前一直收不了钱 ⚠️
- 之前 Claude/WorkBuddy 一直在修当面付代码、配密钥、走沙箱——**方向错了**。
- 真正卡点：支付宝后台「可调用产品」明确提示 **「未查询到账号 128***@qq.com 有相关产品调用权限」** → 当面付**根本没开通**，所以调用返回 `ACCESS_FORBIDDEN`。
- 而当面付（以及微信支付、支付宝网页支付等所有"自动到账"产品）**签约必须有营业执照（企业/个体工商户）**。本账号是**个人支付宝，无营业执照 → 永远签不下来**。
- 用户已确认：暂时**没有营业执照**，先用个人收款码过渡；以后办个体户再切回官方自动收款。

### 顺带查出的密钥隐患（转正后要修）
- 服务器 `.env` 里 `ALIPAY_PUBLIC_KEY` 填的是 `mY1O…` 开头的**孤儿公钥**，跟服务器实际私钥（`密钥20260625122355`，派生公钥 `pn+NzOLw…`）**不配对**，且本该填"支付宝公钥"而非应用公钥。三把钥匙是乱的，以后走官方收款必须重配。
- 已生成配对的正确应用公钥：`~/Desktop/支付宝_应用公钥_上传这个.txt`（`pn+NzOLw…`）。

---

## 二、过渡方案：个人收款码 + 后台人工开通
**客户流程**：登录 → 开通会员 → 弹窗显示收款码+金额 → 扫码付款 → 把账号+付款截图发客服微信 → 点「我已付款」(生成待确认订单)。
**店主流程**：打开 `http://159.75.130.35/admin.html` → 输管理员密码 → 看到待处理订单 → 确认收款点「开通」→ 客户刷新即会员。

---

## 三、本次改了什么（已在本仓库 main 分支）
**后端 `server.opt.js`**（部署到 `/opt/xinmeiti-app/server.js`）新增：
- `GET  /api/pay/qr`          收款码图片（读 `/root/.xinmeiti/pay-qr.jpg`）
- `GET  /api/pay/manual`      返回客服联系方式+套餐
- `POST /api/pay/manual-order` 用户「我已付款」→ 生成待确认订单
- `POST /api/admin/orders`    管理员查所有订单（凭密码）
- `POST /api/admin/activate`  管理员一键开通会员（复用原 member/member_until 逻辑）
- `POST /api/admin/lookup` / `/api/admin/revoke`
- 原当面付接口 `/api/pay/create` 等**保留不动**，以后转正可切回。

**前端 `src/renderer/index.html`**：支付弹窗从"当面付扫码+轮询"改为"收款码+客服+我已付款"；`openPay()` 重写，新增 `manualPaid()`。

**新增 `src/renderer/admin.html`**：管理后台页（凭密码看订单/一键开通）。

**`deploy/`**：`pay-qr.jpg`（收款码）、`deploy_pay.sh`（远程部署脚本）。

---

## 四、⚠️ 部署难题（下次要解决的头号问题）
**现象**：跑了部署命令，脚本内部 `curl 127.0.0.1` 验证成功（返回了新接口），但**外网 `159.75.130.35` 仍是旧版**，过一会儿连服务器上的文件也变回旧版（`server.js` 无 manual 接口、`index.html` 78009 字节旧版）。

**已排查**：
- `/opt/xinmeiti-app` **不是 git 仓库**，**无可疑 cron** → 排除自动还原。
- pm2 进程 `xinmeiti` 跑 **`/opt/xinmeiti-app/prod-server.js`** → `require('./server.js')`，端口 **8787**。
- nginx：静态根 `/opt/xinmeiti-app/src/renderer/`，`/api` 反代 `127.0.0.1:8787`，`listen 80`，`server_name _`。
- pm2 `restart_time=12`（重启偏多）。

**最可能原因**：**多 AI 工具互相覆盖部署**（用户同时用 WorkBuddy/Codex/Claude，曾发生过"回退"）。我部署后，疑似 WorkBuddy 那边用旧版又覆盖回去了。

**对策（下次务必遵守）**：
1. **GitHub `github.com/lnzhihao/xinmeiti-app` 为唯一真相源**，本次已把最新代码 push 上去。
2. **部署从 git 走**：服务器 `cd /opt/xinmeiti-app && git pull`（但 /opt 当前非 git，需先 `git init`+关联 remote，或继续用 tar 推包）。
3. **一次只用一个 AI 改/部署**，部署后**立刻从外网** `curl http://159.75.130.35/api/pay/manual` 验证，确认是新版再收工。

---

## 五、下一步：正确部署 + 外网验证（精确步骤）
部署包脚本在 `deploy/deploy_pay.sh`，它会：备份 → 覆盖 `server.js`/`index.html`/`admin.html`/`pay-qr.jpg` → 写 `.env`(PAY_CONTACT/ADMIN_PASSWORD) → `pm2 restart` → 内部验证。

> ⚠️ 注意：pm2 跑的是 `prod-server.js`（它 require `./server.js`），所以覆盖 `/opt/xinmeiti-app/server.js` 是对的，重启即生效。

**部署命令**（用户终端跑，微信扫码；包已在本地 `/tmp/xm-pay.tgz`，或用 deploy/ 重新打）：
```bash
cd ~/Desktop/新媒体-app/deploy && tar czf /tmp/xm-pay.tgz -C . . && \
ssh root@159.75.130.35 'mkdir -p /tmp/xmpay && cd /tmp/xmpay && cat > p.tgz && tar xzf p.tgz && cp server.js /opt/xinmeiti-app/server.js && cp index.html admin.html /opt/xinmeiti-app/src/renderer/ && mkdir -p /root/.xinmeiti && cp pay-qr.jpg /root/.xinmeiti/ && pm2 restart xinmeiti && cd / && rm -rf /tmp/xmpay' < /tmp/xm-pay.tgz
```
（注意：上面的 tar 需包含 server.js/index.html——deploy 目录里目前只有 qr 和脚本，实际部署要把 server.opt.js→server.js、index.html、admin.html 一起打包。最稳的是直接复用根目录文件。）

**部署后必做外网验证**：
```bash
curl -s http://159.75.130.35/api/pay/manual          # 应返回 {"ok":true,"contact":...}
curl -s -o /dev/null -w "%{http_code}" http://159.75.130.35/admin.html   # 应 200
```
**两个都对，才算真生效。**

---

## 六、配置 / 凭据
- 客服微信：`13352774776`（`.env` 的 `PAY_CONTACT`，部署脚本会写）
- 管理后台：`http://159.75.130.35/admin.html`，密码 `songshu2024`（`.env` 的 `ADMIN_PASSWORD`）
- 🔴 **上线前务必改掉默认管理员密码**（代码里有兜底默认值 `songshu2024`，请在服务器 `.env` 设强密码覆盖）。

---

## 七、用户其他待办（地基稳了再做，按其口述）
1. **Logo**：浏览器标签是地球=没设 favicon；侧边栏也要换真 logo（用户有 logo，待提供文件）。要**极简、单一形状、不写实、不卡通**。
2. **会员页/Token**：当前能看不能点；要一个**真能用的 Token 用量页**（看用了多少、每日多少）。
3. **UI 精致化**；**配色**：不喜欢侧边蓝，想换方向（待定：墨黑+琥珀金 / 炭灰+祖母绿 / 白+原木棕）。
4. **右下角客服**：加微信+在线状态，接一个能聊的渠道；并通盘检查网站逻辑。
5. **会员按钮重叠**（「免费用户」和「充值会员」叠在一起）→ 重做侧边栏底部布局。
6. 目标：给朋友/客户做推广售前，要好看；每步自检 1–2 次再发版。
